#include <stdio.h>
#include <stdbool.h> 
int main(){
    // bool is =false;
    // printf("%d\n",is);

    int age=10;
    if(age>=18){
        printf("Your can Vote\n");
    }
    else{
        printf("Your can't Vote\n");
    }


    return 0;
}